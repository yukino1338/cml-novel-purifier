[tool.coverage.run]
branch = true
relative_files = true
source = ["scripts"]

[tool.coverage.json]
output = "coverage.json"

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
